# 插件工作区规则

本仓库维护 SynapsePro 本地修改版，插件源码位于 `addon/`。

- 保留 `original/` 原始副本和既有用户数据。测试使用 `runtime/` 独立配置，不操作默认 Anki2。
- 修改前检查工作区状态，保留其他未提交修改；按变更执行相关回归和事件循环冒烟测试。如实报告失败或未验证部分。
- 用户已授权每次本地提交后推送到现有 `https://github.com/JMAY666/ANKI.git` 仓库的独立插件分支 `codex/synapsepro-custom`，无需重复请求推送权限。
- 提交前审核差异与敏感内容，明确暂存路径；推送前核实远程地址和完整待推送范围（包含历史中已删除的文件）。排除环境、运行数据、备份、凭据与构建产物。
- 推送只更新 `codex/synapsepro-custom`；不推送到远程 `main`，不强制推送、不改写已有历史、不自动合并到主分支。若远程分支出现其他人的新提交，先检查并保留其工作。
- 远程 main 是 Anki 主程序历史；插件分支保留自己的独立根历史，不把两个代码树混合。
- 涉及安装或配置变更时同步更新 README；完成后报告验证结果、提交哈希和实际推送结果。
