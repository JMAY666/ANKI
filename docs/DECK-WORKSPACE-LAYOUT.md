# 牌组页面布局

牌组目录与主内容之间支持拖动分隔条。目录默认宽 250 像素，范围为 190–480 像素，并为学习区域保留至少 320 像素。窗口缩小时自动收紧目录，恢复窗口宽度后还原用户选择；网页可用宽度不超过 650 像素时改为上下排列。

悬停显示左右调整光标，拖动实时更新，松开停止。分隔条可以通过 Tab 聚焦，左右方向键每次调整 10 像素，Shift 加速至 40 像素，Home/End 调到当前边界，双击恢复默认。宽度随原有目录状态保存在当前网页会话中，切换牌组或进入学习后返回保持；关闭软件后使用默认宽度。

辅助信息、账户等级、连击、每日挑战、升级进度、每日知识及统计放在学习面板下方。普通与简洁面板根据主内容的可用宽度分列，不再占用独立右栏。原有折叠状态、账户组件、统计口径和命令入口保留。

页面布局不会写入卡片或调度数据，也不更改学习队列、每日限制或现有设置。

## 验证

`just deck-workspace-smoke <独立运行名>` 在离屏 Qt/WebEngine 与合成账户中验证布局、按住鼠标实时拖动、宽度边界、窗口缩放、搜索、选中、折叠、开始学习和学习数据保留。`restart` 使用同一合成账户复核；`inspect` 可供用户自行打开测试窗口。本次未操作正式 Anki 窗口，人工界面验收由用户完成。测试数据及截图仅保存在忽略的 `runtime/`。

2026-09-13 自动检查结果：

- 源码回归 58 项通过；软件包首次运行和重启各 58 项通过，涵盖普通／简洁面板、深色主题、480–1400 像素窗口和 150% 网页缩放。牌组切换保留账户面板节点，合成卡片的牌组、队列、到期及复习次数保持一致。
- `just build` 与 `just builtin-package` 成功。软件包内的页面 Python 文件、生成的 CSS 和 JS 与当前源码／构建产物 SHA-256 一致。
- `just check` 未完全通过：629 项 Rust、153 项 Python 库、78 项前端和 180 项 Qt 测试通过，格式、类型和 Svelte 检查通过；唯一剩余失败为原有 `test_build_and_package` 的 WiX MSI 解包环境问题。本次可运行软件包使用现有运行时和新构建的 wheels，不依赖此 MSI 打包步骤。
- 原始全量检查也被上游贡献者登记检查拦截。补跑使用检查器已有的 `CONTRIBUTORS_BYPASS_EMAILS`，没有修改 CONTRIBUTORS 或许可声明。Windows 测试子进程编码导致的捕获错误通过进程级 `PYTHONUTF8=1`、`PYTHONIOENCODING=utf-8`、`PYTEST_ADDOPTS=--capture=sys` 处理，未跳过测试。
- 离屏测试仍有 Qt/GPU/font 和退出回调提示；实际桌面显示、系统缩放和主题热切换留给用户验收，不将离屏结果描述为人工验收通过。

## 本机验收版本

新版本位于 `dist/Anki-deck-layout-26.8.1/Anki.exe`。先正常退出旧版 Anki，再从新目录启动，沿用原有账户数据；本次没有复制或迁移正式学习数据，也没有覆盖正在使用的程序。验收后如果需要回到旧版，退出新版本后打开原目录 `dist/Anki-experience-final-26.8.1/Anki.exe` 即可。

新包保留当前工作区已经存在的功能移除状态；Git 提交仅纳入本次布局相关文件和文档，已有“移除学习计划”的未提交修改保持原状。`.gitignore` 无需新增规则，测试数据、软件包、配置与备份不纳入提交。
