# 接手准备结果 — 2026-09-11

**后续补测更新：** 已完成真实事件循环冒烟测试：功能阶段 27 项通过、思维导图相关 3 项失败；独立重启 15 项通过。详情与证据见 [SMOKE.md](SMOKE.md)。下文“未验证完整 profile/复习/笔记”等为首次交接时的状态，以补测报告为准。

代码位置：`E:\AI项目\ANKI\synapsepro-236979321\addon`。原版 1.5.0 的源码与资源未修改。现有 `E:\AI项目\ANKI\ANKI` 主程序仓库起始工作区干净，本阶段不在其中构建、修改或提交。

## 已验证

- 官方下载成功，ZIP 路径检查通过，保留完整原包与 SHA256。
- 作者仓库已完整克隆；发布文件与作者源码规范化换行后相同。原包到工作副本逐字节一致。
- 38 个 Python 文件 AST 解析、3 个 JSON 解析成功。
- 独立 Python 3.13.13 + Anki/aqt 25.09.4 环境安装完成，37 个依赖锁定，`uv pip check` 通过。
- 两套隔离 profile 创建成功；合成牌组和一张合成 Basic 卡片在 collection 关闭/重新打开后仍存在。
- 使用 Anki 专门供测试的 `_run(..., exec=False)` 建立 Qt 应用。Anki 插件管理器实际导入 ID `236979321`，`modules_loaded=True`，`profile_did_open` 注册成功；最终探针退出码 0。
- 结果文件 `runtime/load-probe/load-result.json`，日志 `runtime/load-probe/load.log`。探针使用 offscreen，无事件循环，不作为界面验收。

## 未验证与现有问题

- 未验证可见 UI、完整 profile 初始化、首次引导、复习/XP、笔记/PDF、思维导图、音乐或 AI 请求。当前会话的原生桌面 UI 控制不可用，未用其他 UI 自动化方式绕过。首次引导尚需本人审阅条款，未代为同意或伪造“已完成”设置。
- 未声称 Anki 26.08.1 兼容：超过 AnkiWeb 分支上限 26.05.0。仅实测 25.09.4 的模块加载层；也未覆盖所有 25.09.4–26.05.0 版本。
- PyPI 环境 Qt 6.11.2 是此次锁定解析结果，与作者/历史官方安装包的 Qt 版本不保证一致。正式 UI 验证后才能给出此组合的完整兼容结论。
- 原版 locales.py 有 8 个 `\!` 无效转义 SyntaxWarning；加载存在 `night_mode()` / `currentLang` 弃用警告。offscreen Qt 报字体目录警告；这些没有导致最终导入失败，本阶段未修复源码。
- 初始导入执行 `git diff --cached --check` 检出原版尾随空格等格式问题；为保持原始基线逐字节一致，记录并保留，没有把该检查报告为通过。后续本阶段新增工具/文档单独检查。
- 首次探针因测试脚本误把 hook 对象当作 iterable 失败，随后修正为读取其 `_hooks`；另修正 Anki stderr 包装器不支持 flush 的探针收尾问题。只改测试工具，最终重跑成功。
- 没有上游自动化测试配置；AST/依赖/加载检查不能替代功能回归。未执行原 Anki 仓库的 `just check`，因为该仓库无变更且本阶段测试对象为独立发布插件。
- Git 基线只在本地创建，没有添加工作仓库 remote，没有推送。`original/upstream` 自带作者 origin，仅用于读取。
- 原版基线提交 `8710fb8`，标签 `baseline-1.5.0`。另一个准备提交仅包含文档、环境锁定与测试/打包脚本。已生成本地安装包，ZIP 校验通过；未把打包成功等同于手动安装成功。
- 原包许可证对代码为 MIT，但图片、音频及品牌资源另有保留权利条款。当前保留本地原版供接手；后续涉及公开发布或素材改造时需按实际许可处理。

## 数据保护范围

所有创建的牌组、卡片、profile、插件运行副本均在本工作区 `runtime/`；未打开默认 Anki2 collection，未读取/复制个人牌组、媒体、学习记录、配置或密钥。未登录同步服务。未做正式数据目录的全量前后哈希审计，因此不宣称完成该审计。

首次引导和手动功能验证步骤已写入 README；后续可以直接围绕 `addon/` 接受具体修改要求。
