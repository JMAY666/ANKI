# 合并至 main（2026-09-11）

用户已将工作方式改为：完成修改并审核后，直接提交、推送到 `JMAY666/ANKI` 的 `main`。此前仅推送插件分支的规则不再适用。

远程合并前只有两个分支：主程序 `main`（`e52185e4`）和插件 `codex/synapsepro-custom`（`3624f28`）。二者原先没有共同祖先。本次使用保留双方历史的合并提交，不强制推送，不重写旧提交。

- 保留主程序 `qt/`、`pylib/`、`rslib/`、`ts/`、四个子模块及构建文件。
- 插件保留在 `addon/`，其工具保留在 `scripts/`、测试保留在 `tests/`。没有把插件目录替换成主程序，或把主程序文件替换成插件。
- 主 README 保留 Anki 说明并增加插件入口；原插件 README 整理到根 `SYNAPSEPRO.md`。
- 主程序的 `docs/architecture.md` 保留；插件的 `docs/ARCHITECTURE.md` 改为 `docs/SYNAPSEPRO-ARCHITECTURE.md`，避免 Windows 不区分大小写造成文件覆盖。
- 合并 Git 排除规则，保留上游测试数据的精确例外；环境、运行配置、原始包和备份不入库。
- 主程序的版权头/格式检查排除有独立版权及验证流程的插件目录与插件工具，不给第三方源码强行添加 Ankitects 版权头。主程序代码仍执行原有检查。
- 部署脚本引用已保留的原始提交 SHA，不依赖未上传的本地标签。

验证：`just fix-fmt`、增量 `just check` 和插件 7 项离线回归测试通过。首次检查报告的独立插件版权头、格式差异已按上述边界处理后重跑通过；缺少可选 `.claude/user.md`，使用仓库既有验证文档中的环境配置。插件 `addon/` 的全部文件与合并前分支逐字节相同，原主程序源码及子模块对象保持相同（仅调整检查范围和仓库文档）。

主分支相对父提交的差异包含已有插件代码，因此完整 `git diff --check` 仍会报告插件原有尾随空格；不改动这些继承内容，新增合并文件另行检查。没有把继承的格式警告描述为新业务缺陷或完全无警告。

旧分支保留为已合并的历史引用；后续统一在 `main` 工作。没有删除用户数据、远程分支或本地备份。
